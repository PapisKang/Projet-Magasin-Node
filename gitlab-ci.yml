stages:
  - build
  - test
build:
  stage: build
  image: node:latest
  script:
    - npm i && npm run build
  artifacts:
  paths:
    - build/
    

test:
  stage: test
  image: node:latest
  script:
    - npm i && npm test -- --coverage
  coverage: /All files\s*\|\s*([\d\.]+)/